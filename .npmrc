workspaces=false
# 의존성 lifecycle 스크립트(postinstall 등) 차단 — 적대 리뷰 2026-08-29.
# 에이전트의 npm install이 레지스트리 제3자 코드를 무스코프로 실행하는 경로를 닫는다
# (환각 패키지명 + 타이포스쿼팅 조합). 템플릿 의존성 중 postinstall 필수는 없고,
# playwright 브라우저는 별도 명령(npx playwright install)으로 설치된다.
ignore-scripts=true
